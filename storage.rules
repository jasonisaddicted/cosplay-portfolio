rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read with public cache headers, authenticated write
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
  }
}
